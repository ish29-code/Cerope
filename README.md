# MERN Auth + Profile App  

Simple MERN stack app with Signup, Login, Profile Setup, and Edit Profile features.  
Run backend on `localhost:5000` and frontend with `npm run dev`.  
Pages: `/register`, `/login`, `/setup`, `/profile`.
